query getProducts($limit: Int, $offset: Int) {
  products(limit: $limit, offset: $offset) {
    id
    title
    price
    isShippable
    isAuction
    auction {
      expirationTime
    }
    currency {
      currency_code
      currency_symbol
    }
    images {
      uri
    }
  }
}
